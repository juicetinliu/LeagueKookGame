export class Question {
    constructor(uniqueId, title, answer, image) {
        this.uniqueId = uniqueId;
        this.title = title;
        this.answer = answer;
        this.image = image;
    }
}